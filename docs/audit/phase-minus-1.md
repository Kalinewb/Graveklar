# Test audit 2026-09 — Phase −1: reality check

Read-only checks against the production host, run 2026-09-06 before any test was written.
Findings are numbered R-n; they feed `docs/test-audit-2026-09.md` at the end of the audit.

| # | Check | Result | Finding |
|---|---|---|---|
| 1 | Is a scheduler calling `/api/cron/cleanup` and `/api/cron/reminders`? | Unit files exist (`~/.config/systemd/user/graveklar-{cleanup,reminders}.{service,timer}`) but both timers are **disabled and inactive**. Last successful runs in the journal: 2026-07-14. Cleanup still runs lazily from `/api/availability` and `GET /api/bookings` traffic; reminders have not been sent since July. | **R-1 (resolved 2026-09-06):** owner enabled both timers; `list-timers` shows them active (cleanup every 10 min, reminders daily 08:00, Persistent). Flag F-17 confirmed and closed. **Caveat R-1b:** the cleanup timer uses only `OnBootSec=2min` + `OnUnitActiveSec=10min`; enabled after boot it shows `NextElapseUSecMonotonic=infinity` until the service runs once (`systemctl --user start graveklar-cleanup.service`), and the same happens after every reboot where the 2-minute mark passes before the timer is active. Recommended: replace both lines with `OnCalendar=*:0/10` (keep `Persistent=true`), then `daemon-reload` and restart the timer. |
| 2 | Is the deployed process using the expected DB and TZ? | `graveklar.service` active + enabled, `next start -p 3000`, `DATABASE_URL=file:…/prisma/prisma/db/custom.db`, `TZ=Europe/Oslo`, env from `.env`. Build ID written 17:56 after HEAD `1bc2487` at 17:53, so prod matches HEAD. | none |
| 3 | Can protected routes be reached around the proxy from outside? | `/api/admin/app-config` → 401; upper-case path → 404; `%61dmin` → 404; double slash and trailing slash → 308 to the canonical path; `GET /api/bookings` → 401; `GET /api/config` → 200 (public by design, F-12). No matcher gap found from outside. | none from outside; handler-level probes stay in Phase 1d |
| 4 | Are Stripe/Vipps production credentials separated from test? | Live AppConfig holds `sk_test_…` / `pk_test_…` Stripe keys with `stripeEnabled=true`, and `vippsEnabled=true` with `vippsEnvironment=test`. Both public status endpoints on graveklar.no return `{"enabled":true}`. | **R-2 (accepted 2026-09-06):** the owner confirmed test keys for Stripe and Vipps are intentional at this stage (pre-launch). No action; re-check before go-live. |
| 5 | Is the upload directory publicly reachable? | `GET /api/uploads/<name>` answers 404 for a missing file and serves any existing file without authentication. | F-2 confirmed as designed; authorization tests in Phase 1d, fix in Phase 1.5 |
| 6 | What sets the client IP the rate limiters key on? | `server: cloudflare` and `cf-ray` on every response, HSTS present. No `cloudflared`, nginx or caddy on this host; only `next-server` listens, on `0.0.0.0:3000`. A direct request to `<egress ip>:3000` from the host did not connect (NAT hairpin, inconclusive). | **R-3 (P0 analysis input for F-7):** Cloudflare appends the connecting IP to any client-supplied `X-Forwarded-For` and does not strip `X-Real-IP`; the limiters read `x-real-ip` first and `x-forwarded-for[0]` second, both attacker-controlled. The correct identity behind Cloudflare is `CF-Connecting-IP` (or the last XFF hop), and the origin should only accept traffic from Cloudflare. Tested in Phase 1d, fixed in Phase 1.5. |
| 7 | Are backups scheduled? | `backups/` holds one manual copy from 2026-09-04 (pre-Vipps). No timer or cron runs `scripts/backup-db.sh`. | **R-4 (P1, ops):** schedule the backup script (a user timer, daily) and keep retention. |
| 8 | Is the production webhook endpoint the one Stripe calls? | One enabled endpoint at `https://graveklar.no/api/payment/stripe/webhook`, API version 2026-04-22, subscribed to 4 events: `checkout.session.completed`, `.async_payment_succeeded`, `.async_payment_failed`, `.expired`. The handler also processes `charge.refunded`, `charge.dispute.created`, `payment_intent.payment_failed`. | **R-5 (P1):** refund and dispute bookkeeping never arrives. Add the three events to the endpoint in the Stripe dashboard (or via API). |
| 9 | Is `DATABASE_URL` SQLite in every environment? | `.env` (prod), `docker-compose.yml`, `Dockerfile` build stub, `ci.yml` stub, and `.claude/launch.json` (audit instance) all use `file:` URLs. `.env.example` documents it. | none; `.env.example` still lacks `CRON_SECRET` and `RENTER_SESSION_SECRET` (R-6, P3) |
| 10 | Is the branch relationship as assumed? | `main` and `dev` both at `1bc2487`, 0 commits apart, both 8 ahead of origin (unpushed by instruction). | none |

## Decisions taken from this phase

- F-17 is confirmed and becomes R-1; the fix is a two-command operational change handed to the owner, not a test.
- F-7's production case is "Cloudflare in front, header pass-through": the Phase 1d tests model exactly that.
- R-2 was reported and the owner accepted it the same day: test keys are planned. Closed; goes on the go-live checklist.
